{
  "name": "node_web_scraper",
  "version": "1.0.0",
  "description": "command line program that can parse remote pages, read the html, and write the content that we care about to a file on disk",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Natasha",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^0.20.0",
    "request": "^2.74.0"
  }
}
